此目录包含Cora数据集（www.research.wwhibbang.com/data）的选择。
Cora数据集由机器学习论文组成。这些论文分为以下七类之一：
基于案例（_B）
遗传算法
中性_网络
概率_方法
强化_学习
规则_学习
学说
论文的选择方式是，在最终的语料库中，每篇论文都引用或被至少一篇其他论文引用。整个语料库共有2708篇论文。
在词干和去除词尾后，我们只剩下1433个独特单词。所有文档频率小于10的单词都被删除。
该目录包含两个文件：
.content文件包含以下格式的论文描述：
<paper_id><word_attributes>+<class_label>
每行中的第一个条目包含论文的唯一字符串ID，后跟二进制值，指示词汇表中的每个单词在论文中是存在（用1表示）还是不存在（用0表示）。
最后，行中的最后一个条目包含论文的类标签。

cites文件包含语料库的引文图。每行以以下格式描述一个链接：
＜引用论文ID＞＜引用论文的ID＞
每行包含两个纸张ID。第一个条目是被引用论文的ID，第二个ID代表包含引用的论文。链接的方向是从右向左。如果一条线由“paper1-paper2”表示，则链接为“paper2->paper1”。